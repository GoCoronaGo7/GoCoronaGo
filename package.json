{
    "name": "gocorongoapp",
    "version": "0.1.0",
    "private": true,
    "engines": {
        "node": "^16.6.1",
        "yarn": "^1.22.10"
    },
    "scripts": {
        "build": "cd client && pnpm install && pnpm build",
        "postbuild": "cp -r ./client/build ./static/"
    }
}
